pipeline {
  agent any

  stages {
    stage('Hello') {
      steps {
        echo 'Hello'
        sh 'echo "command in stage 1"'
      }
    }
  }
  post {
    always {
      echo 'I will always run'
    }
    success {
      echo 'Tests passed successfully'
    }
    failure {
      echo 'Tests failed'
    }
  }
}
